Options Framework Modification
------------------------------
Look for the following inside the options-framework.php file:
    $optionsfile = locate_template( $location )
And replace the value with plugin's base directory
